---
import Image from "astro/components/Image.astro";
---

<figure class="overflow-hidden relative group/image-hero rounded-2xl shadow-2xl shadow-primary/20 my-8">
  <div class="relative">
    <Image
      height={300}
      width={500}
      loading="lazy"
      class="select-none group-hover/image-hero:scale-110 aspect-video size-full transition duration-700 object-cover z-1 inset-0"
      src="/hero-anime-info.png"
      alt="Descubre y explora tus series de anime y manga favoritas"
    />
    {/* Overlay gradiente desde abajo */}
    <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent"></div>
  </div>
  
  <figcaption
    class="bg-white/95 backdrop-blur-sm max-w-fit text-primary text-sm px-4 py-2 rounded-full absolute bottom-4 left-4 z-10 shadow-lg font-medium flex items-center gap-2"
  >
    <span class="w-2 h-2 bg-red-500 rounded-full animate-pulse"></span>
    <span>Your Name: <i class="font-semibold">Kimi no Na wa</i></span>
  </figcaption>
</figure>
